#ifndef ENUM_HPP
#define ENUM_HPP

enum Ingredients{
    MILK,
    BEANS,
    WATER,
    SUGAR,
    CARAMEL
};

enum VendingMachineState{
    IDLE,
    SELECT_COFFEE,
    INSERT_MONEY,
    PROCESSING,
    DISPENSE
};

#endif